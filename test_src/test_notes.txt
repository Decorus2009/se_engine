1. Тестирование предлогов.
    Сами предлоги нигде в text_analyzer.cpp не хранились, для них создал временный вектор, содержиое которого тестируется теперь.

2. Для того, чтобы работало быстрее в text_analyzer.cpp временно закомментировал строку, которая посылает запросы (308 строка).
